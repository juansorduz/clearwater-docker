This folder has the files to configurate kubernetes2 deployment (transitory deployment between clearwater over docker and uvIMS) that starts with the following commands:
  cd clearwater-docker
  kubectl apply -f kubernetes2
